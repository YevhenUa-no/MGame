# Copy this to .env (local) or set as a Vercel project env var.
# Point this at wherever server/server.js is actually running.
# Local dev default (server.js falls back to this if unset): ws://localhost:8787
VITE_MULTIPLAYER_URL=wss://your-server.up.railway.app
